# Consistent frame dim + divider width (2026-08-07)

* All box-drawing glyphs use dim; content keeps its own colors.
* Dividers/edges/content rows share one width formula via display columns (not UTF-8 byte length).
